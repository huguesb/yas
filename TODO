 * documentation (README + doxy)
 * better error reporting in exec.c
 * tests
 * check background task CPU utilization (looks kinda wrong...)

?
 * user variables
 * special variables ($0..$n $? $@)
 * >> 2>1 &> ... (general redir revamp)
 * && ||
 * arith expansion
 * logic, control flow, functions...

